def reply(comment_text, video_url):
    """Return the text that should be posted as a reply.

    This is intentionally tiny for now. Later this function can call an LLM
    with the same small input shape.
    """
    return "copy that"
